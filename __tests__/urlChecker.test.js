import { checkForURL } from "../src/client/js/urlChecker"


describe("test: if text is URL", () => {
    
    test("test function", () => {
          
           expect(checkForURL).toBeDefined();
})});